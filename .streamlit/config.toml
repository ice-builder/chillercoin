[theme]
# Using "custom" theme allows full CSS control.
# Users can switch between Light/Dark via Streamlit Settings menu (☰ → Settings → Theme).
primaryColor = "#0ea569"
backgroundColor = "#0b0d12"
secondaryBackgroundColor = "#12151d"
textColor = "#f0f2f5"
font = "sans serif"

[server]
headless = true
